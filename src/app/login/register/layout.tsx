import React from "react";

export default function RegisterPageLayout({children} : {children :React.ReactNode}){
return (
    <html>
        <head>

        </head>
        <body>
        
    {children}
        </body>
    </html>
)

}